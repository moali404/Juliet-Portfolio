apiVersion: v1
kind: Service
metadata:
  name: juliet-service
  labels:
    app: juliet
spec:
  selector:
    app: juliet
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  # type: LoadBalancer  # Or NodePort/ClusterIP depending on your environment
